import type { NextPage } from 'next'

const Home: NextPage = () => {
  return (
    <div
      style={{
        display: 'flex',
        width: '100%',
        height: '100vh',
        padding: 0,
        margin: 0,
        justifyContent: 'center',
        alignItems: 'center'
      }}
    >
      You have to define path parameter
    </div>
  )
}

export default Home
